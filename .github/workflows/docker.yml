name: Docker - Build & Push
on:
  push:
    branches:
      - main
      - "feature/**"
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref (tag or branch) to build"
        required: true
        default: "main"
concurrency:
  group: docker-${{ github.ref }}
  cancel-in-progress: false
env:
  GHCR_IMAGE: ghcr.io/${{ github.repository_owner }}/imou-ptz
jobs:
  build-and-push:
    name: Build & Push
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Set up QEMU (multi-arch)
        uses: docker/setup-qemu-action@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Resolve ref
        id: resolve
        run: |
          REF="${{ github.event.inputs.ref || github.ref }}"
          echo "REF=${REF}" >> "$GITHUB_OUTPUT"
      - name: Sanitise feature tag
        if: startsWith(steps.resolve.outputs.REF, 'refs/heads/feature/')
        id: feature-tag
        run: |
          RAW="${{ steps.resolve.outputs.REF }}"
          RAW="${RAW#refs/heads/}"
          echo "TAG=${RAW//\//-}" >> "$GITHUB_OUTPUT"
      - name: Build & push to GHCR (feature)
        if: startsWith(steps.resolve.outputs.REF, 'refs/heads/feature/')
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ env.GHCR_IMAGE }}:${{ steps.feature-tag.outputs.TAG }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Build & push to GHCR (:latest)
        if: steps.resolve.outputs.REF == 'refs/heads/main'
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ env.GHCR_IMAGE }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Extract metadata (release tag)
        if: startsWith(steps.resolve.outputs.REF, 'refs/tags/v')
        id: meta-release
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.GHCR_IMAGE }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest
      - name: Build & push to GHCR (versioned)
        if: startsWith(steps.resolve.outputs.REF, 'refs/tags/v')
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta-release.outputs.tags }}
          labels: ${{ steps.meta-release.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
